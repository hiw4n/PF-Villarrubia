import { StudenNameLastnamePipe } from './studen-name-lastname.pipe';

describe('StudenNameLastnamePipe', () => {
  it('create an instance', () => {
    const pipe = new StudenNameLastnamePipe();
    expect(pipe).toBeTruthy();
  });
});
